spring:
  application:
    name: discovery
server:
  port: 8761


eureka:
  instance:
    hostname: viniciuscluna-discovery.herokuapp.com
  client:
    fetch-registry: false
    register-with-eureka: false